# راه‌اندازی بک‌اند تیسافت روی Vercel (قدم‌به‌قدم)

بک‌اند شامل: **ثبت‌نام/ورود کاربران**، **چت آنلاین با پشتیبانی**، **سیستم تیکت**، **فراموشی رمز عبور** و **فرم تماس (ارسال به ایمیل)** است.

دو سرویس رایگان باید فعال شوند: **Vercel KV** (برای ذخیره‌ی داده‌ها) و **Resend** (برای ارسال ایمیل).

---

## مرحله ۱ — فعال‌سازی Vercel KV (دیتابیس)

۱. وارد داشبورد [vercel.com](https://vercel.com) شوید و پروژه‌ی سایت را باز کنید.
۲. به تب **Storage** بروید.
۳. روی **Create Database** کلیک کنید و گزینه‌ی **KV (Redis)** را انتخاب کنید.
۴. یک نام بگذارید (مثلاً `tsoft-db`) و **Create** را بزنید.
۵. در مرحله‌ی بعد از شما می‌پرسد به کدام پروژه وصل شود — پروژه‌ی سایت را انتخاب کنید.

✅ با این کار، متغیرهای `KV_REST_API_URL` و `KV_REST_API_TOKEN` به‌صورت خودکار به پروژه اضافه می‌شوند.

---

## مرحله ۲ — فعال‌سازی Resend (ارسال ایمیل)

برای **فراموشی رمز عبور** (ارسال کد به ایمیل کاربر) و **فرم تماس** (ارسال پیام به ایمیل شما).

۱. به [resend.com](https://resend.com) بروید و با ایمیل `bigonahi@gmail.com` ثبت‌نام کنید (رایگان — روزانه ۱۰۰ ایمیل).
۲. از منوی سمت چپ به **API Keys** بروید و روی **Create API Key** بزنید.
۳. کلید ساخته‌شده را کپی کنید (با `re_` شروع می‌شود).
۴. به داشبورد Vercel → پروژه → **Settings** → **Environment Variables** بروید.
۵. یک متغیر جدید بسازید:
   - **Name:** `RESEND_API_KEY`
   - **Value:** کلیدی که کپی کردید
   - برای هر سه محیط (Production, Preview, Development) تیک بزنید و **Save** کنید.

### مهم: تأیید ایمیل فرستنده
در حالت پیش‌فرض، ایمیل‌ها از `onboarding@resend.dev` فرستاده می‌شوند که **فقط به ایمیل خودتان** (همانی که در Resend ثبت‌نام کردید) قابل ارسال است — برای تست کافی است.

برای ارسال به **هر ایمیلی** (مثلاً ایمیل مشتریان برای فراموشی رمز)، باید دامنه‌ی `tsoft20.ir` را در Resend تأیید کنید:
- در Resend به **Domains** بروید → **Add Domain** → `tsoft20.ir` را وارد کنید → رکوردهای DNS داده‌شده را در تنظیمات دامنه اضافه کنید.
- بعد از تأیید، در Vercel یک متغیر محیطی دیگر بسازید:
  - **Name:** `EMAIL_FROM`
  - **Value:** `تیسافت <no-reply@tsoft20.ir>`

---

## مرحله ۳ — متغیرهای اختیاری

در همان بخش Environment Variables می‌توانید این‌ها را هم تنظیم کنید (اختیاری):

| متغیر | مقدار | کاربرد |
|---|---|---|
| `CONTACT_EMAIL` | `bigonahi@gmail.com` | ایمیل دریافت‌کننده‌ی فرم تماس (پیش‌فرض همین است) |
| `ADMIN_PHONE` | شماره موبایل شما (مثلاً `09153133726`) | این شماره هنگام ثبت‌نام، خودکار مدیر می‌شود |

---

## مرحله ۴ — دیپلوی نهایی

۱. کد را push کنید:
```bash
git add .
git commit -m "سیستم حساب کاربری، چت، تیکت و فراموشی رمز"
git push
```
۲. Vercel خودکار دیپلوی می‌کند. چند دقیقه صبر کنید.

---

## ساخت حساب مدیر

- **روش خودکار:** اگر متغیر `ADMIN_PHONE` را تنظیم کرده باشید، با همان شماره ثبت‌نام کنید → خودکار مدیر می‌شوید.
- **روش اولین کاربر:** اگر `ADMIN_PHONE` تنظیم نشده باشد، **اولین حسابی که در سایت ثبت‌نام شود، مدیر می‌شود.** پس خودتان زودتر از همه ثبت‌نام کنید.

بعد از ورود به‌عنوان مدیر، دکمه‌ی طلایی «پنل مدیریت» در بالای سایت ظاهر می‌شود.

---

## بررسی سالم بودن بک‌اند

بعد از دیپلوی، این آدرس را در مرورگر باز کنید:
```
https://www.tsoft20.ir/api/auth
```
- اگر نوشت `وارد نشده‌اید` → بک‌اند سالم است ✅
- اگر نوشت `بک‌اند فعال نیست...` → مرحله‌ی ۱ (Vercel KV) انجام نشده ❌

---

## خلاصه‌ی قابلیت‌ها

| قابلیت | توضیح |
|---|---|
| ثبت‌نام / ورود | با شماره موبایل + رمز عبور + ایمیل (اختیاری) |
| چت آنلاین | کاربر با پشتیبانی چت می‌کند؛ هر ۴ ثانیه به‌روز می‌شود |
| سیستم تیکت | کاربر تیکت می‌سازد، مدیر پاسخ می‌دهد، تیکت بسته می‌شود |
| فراموشی رمز | کد ۶ رقمی به ایمیل کاربر فرستاده می‌شود (۱۰ دقیقه اعتبار) |
| فرم تماس | پیام مستقیم به ایمیل `bigonahi@gmail.com` ارسال می‌شود |
| پنل مدیریت | آمار، کاربران، همه‌ی تیکت‌ها و گفتگوها |
